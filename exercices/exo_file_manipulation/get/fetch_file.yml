---
  - name: Fetch host file information in my local machine
    fetch:
      src: "{{ ansible_user_dir }}/{{ ansible_hostname }}.txt"
      dest: ./out_data/{{ ansible_hostname }}.txt
      flat: true